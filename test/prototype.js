var prototype = require('../lib/prototype');

// exports.testInitCreatesThreeLives = function(test) {
  // var result = prototype.Game.init();
  // test.equal(prototype.Game.life.length, 3, "init should create 3 lives");
  // test.done();
// };
